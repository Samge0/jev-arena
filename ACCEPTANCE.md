# Jev Arena 验收报告

日期：2026-09-20 · 全流程（分析/规划/构建/测试/验收）由 Hermes 自主执行

## 一、交付物清单

| 交付物 | 位置 | 状态 |
|---|---|---|
| 俄罗斯方块 + 1024 游戏引擎（纯 stdlib，确定性种子） | `games/` | ✅ 7 项单元测试全绿 |
| 三引擎统一驱动（jev API / nanojev 本地 / laya 本地） | `runner/engines.py` + 两个 worker | ✅ 冒烟通过，行为差异与 9 点基准一致 |
| 正式对局录像 12 局（2 游戏 × 2 种子 × 3 引擎） | `web/arena_results.json` (949KB) + `arena_slim.json` (342KB) | ✅ 全部录制完成 |
| 参考站风格三面板回放站 | `web/index.html` + `arena.js` + `arena.css` | ✅ Playwright 实测 PASS，零 console 错误 |
| 站点自动化验证脚本 | `verify_site.py`（截图在 `shots/`） | ✅ 8 项断言全过 |
| 验收报告 + README | 本文件 + `README.md` | ✅ |

## 二、量化验收表

### 游戏引擎（test_games.py，7/7 PASS）
- Tetris：随机局 64 步 horizon 正常终局；构造满板正确判 topped_out；落地数学（I 块 landing_top=13）正确；确定性种子可复现
- 1024：合并数学（2+2→4 得 4 分，4+4→8 得 8 分）精确；冻结局面 legal_actions=[] 判死正确；600 步随机局稳定

### 对局结果（同种子同问题序列，公平对比）

**Tetris**（80 步预算，一方 top out 即终局）：

| 引擎 | seed 2026 | seed 901 | 平均得分 | 消行 | 违约选点 |
|---|---|---|---|---|---|
| Jev | **40 分 / 1 消行 / 24 块** | 0 分 / 13 块 | **20** | **0.5** | 0 |
| NanoJev | 0 分 / 13 块 | 0 分 / 11 块 | 0 | 0 | 0 |
| Laya | 0 分 / 15 块 | 0 分 / 17 块 | 0 | 0 | 0 |

**1024**（240 步预算，无人合并出 8 以上，均未致死）：

| 引擎 | 平均得分 | 最大方块 | 结果 |
|---|---|---|---|
| Jev | 4 | 4 | 存活到上限 |
| NanoJev | 4 | 4 | 存活到上限 |
| Laya | 0 | 2 | 存活到上限（来回平移） |

### 决策延迟（每步，实测均值）

| 引擎 | 延迟 | 说明 |
|---|---|---|
| Jev (API) | 1165–1177 ms | 517 次调用，257,307 input / 17,007 output tokens |
| NanoJev (GPU bf16) | 241–425 ms | 与 GPU1 上 Bonsai2 服务共存 |
| Laya (CPU) | 101–142 ms | 模型已缓存 |

### 站点验收（Playwright 实测，8 项断言全过）

1. 三面板渲染（Jev/NanoJev/Laya）✅
2. 步进按钮 + 概率条数值联动 ✅
3. 播放/暂停 ✅
4. 时间轴跳到终帧 → 终局状态正确（1024: Alive at limit；Tetris: Topped out）✅
5. #tetris hash 路由切换 ✅
6. 战绩表 2 张 × 3 行 ✅
7. 种子切换 2026→901 ✅
8. **零 console 错误** ✅

截图证据：`shots/01~07`（初始/步进/终局/双游戏/战绩表）。

## 三、弯路诚实复盘（8 项，全部当场发现并修复）

1. **Tetris 消行逻辑写反**（`any`  vs `all`：把空行当满行清）→ 单测第 1 次运行即抓住。
2. **Top-out 规则迭代 3 轮**：第一版只查"完全进不来"，第二版负行号静默写穿棋盘，第三版才确立正确规则"落点越出顶边即死"。教训：Tetris 死亡判定比直觉复杂，必须用构造性测试。
3. **1024 legal_actions 状态还原顺序 bug**：先还原再比较导致"移动但无合并"被误判非法 → 随机局崩溃。单测抓住。
4. **1024 选项描述太中性**（首版只有方向词，无信息量）：3 家 240 步原地打转（jev 只会 up/down 交替）。对照参考站方法论（snake 有 planner 提供候选数据）重写描述为"真实后果数据"（合并数/得分/空格数）后 jev/nanojev 立即产生得分行为。**这是本项目最重要的方法论修正：比较决策模型时，问题描述必须提供真实后果，否则测的是措辞敏感度不是判断力。**
5. **前台跑对局两次撞 600s 超时被杀**：改为后台 + 断点续跑（按 game/seed/engine 幂等跳过）+ 每局增量落盘。jev 的 1024 局 240 步 × 1.2s ≈ 5min/局，前台必超。
6. **slim JSON 编码丢信息**：首版把 4x4 棋盘行拼成无分隔符字符串（"241024."），多位数 tile 信息有歧义且空格 '.' 被渲染成珊瑚色实心块——vision 验收截图抓到。改为逗号分隔 + 前端 decodeGrid。
7. **slim 键名不一致**：编码后键名 `g` 与渲染器 `grid` 不匹配 → 棋盘空白。统一保留原键名。
8. **validateData 过严**：tetris engine_failure 帧无 grid 导致整站拒绝加载 → 放宽为"有 grid 则必须合法"，渲染器对无 grid 帧跳过绘制。

## 四、与本地 9 点静态基准的交叉印证

本项目的整局动态对局结论与 2026-09-19 的 9 点静态基准（`jevshoot/COMPARISON.md`）方向一致且更细：
- Jev 质量领先（Tetris 唯一消行者），与静态基准 ~6/6 一致
- NanoJev 次之且延迟低（本次 GPU 空闲时 241ms，比 jev API 快 5 倍）
- Laya 均匀分布/平移行为，与"base 模型近随机"的作者自述及静态基准 2/6 一致

新增发现：**静态基准测不出的"长程行为模式"**——1024 里 laya 会 up/down 来回平移（120/120），jev 偏 up/down、nanojev 偏 left/right，这种"方向癖"只有在整局录制里才可见。

## 五、已知边界

- 1024 的 240 步预算内无人接近 1024 tile；若需更强区分度可提高预算或加入"预合并"评分问题
- Tetris 只测列选择（固定朝向），旋转决策留待后续（问题会变成两级 choice）
- nanojev 用的是根 checkpoint（未微调游戏域），其 0.6B 容量在开放文本棋盘上本就吃力
- API key 在 `.env`（已 gitignore），录像不含 key
