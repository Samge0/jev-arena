# Jev Arena V3 验收报告（Round 3：最佳实践改造）

日期：2026-09-21 · schema `jev-arena-v3` · 协议 `fanout+composite`

## 一、研究结论（改造依据）

通读 TypeSafe 官方文档（how-to-build / fan-out / composite-scoring / parallel-questions cookbook
/ primitives/advanced）+ NanoJev 作者真实游戏提问源码（build_navigation_v3.py、snake_game.py、
rollout_jev_navigation.py），提炼出 5 条我们此前缺失的最佳实践：

| # | 实践 | 来源 | v1/v2 状况 |
|---|---|---|---|
| 1 | **指令写明策略层级**（"先 X，再 Y，平局等价"）| 作者 snake/navigation 指令原文 | 只写 "pick the best move"，模型各自猜 best |
| 2 | **Fan-out**：一次调用并行多问，零延迟代价（官方实测 12.2x 便宜/10x 快，答案不变）| parallel_questions cookbook | 每步只问 1 个 choice |
| 3 | **组合评分**：原子问题 + 代码端固定权重合成 + 置信度路由 | composite-scoring 文档 | 无 |
| 4 | **棋盘渲染带列标尺**（`columns: 0 1 2...` + `row r:` 前缀）| build_navigation_v3.py | v2 才有 row，无列标尺 |
| 5 | **唯一合法动作 → 代码直接执行**，不发模型请求 | rollout_jev_navigation.py forced_decisions | 无 |

## 二、V3 协议实现

```
每步 = 1 次 ask_multi 调用：
  action    choice：策略层级指令（消行 > 防洞 > 低堆 / 合并分 > 大合并 > 空格）
                   + 每选项纯事实描述（落点坐标/消行数/洞数变化/行号）
  risk      noul  ：洞免存在性 / 卡死风险（fan-out 免费附加）
代码端组合评分：argmax 优先；margin<0.05 且 noul 门触发时按描述事实确定性重排
唯一合法动作：直接执行（forced），零成本
```

三引擎同字节请求；nanojev/laya 的 predictor 不支持 noul，自动降级为纯 argmax（组合层兼容）。

## 三、V3 结果（12 局，2 游戏 × 2 种子 × 3 引擎）

### Tetris（朝向×列组合动作 + 层级指令）

| 引擎 | seed2026 | seed901 | 均分 | 消行 |
|---|---|---|---|---|
| Jev | 340 / 8 消行 / 46 块 | 0 / 25 块（rerank×2） | 170 | 4 |
| NanoJev | 0 / 14 块 | 0 / 23 块 | 0 | 0 |
| Laya | **40 / 1 消行 / 22 块** | 0 / 19 块 | 20 | 0.5 |

变化：laya 在层级指令下首次消行（v2 它 0 分）；jev 两 seed 方差大（901 手牌差）。

### 1024（**游戏规则 bug 修复后** + v3 协议）

| 引擎 | seed2026 | seed901 | 均分 | 最大块 | 振荡率 |
|---|---|---|---|---|---|
| **Jev** | **2444 分 / 128 / 240 步存活** | 1564 / 128 | **2004** | 128 | **7.5%** |
| NanoJev | 644 / 64 / 92 步 | 600 / 64 | 622 | 64 | 30% |
| Laya | 1072 / 128 | 1596 / 128 | 1334 | 128 | 26% |

jev 终局棋盘出现规范的降幂蛇形结构（128,64,32,16 / 8,32,16,8 / ...）——
这是 2048 玩家的标准策略形态，模型确实在执行层级指令。

### 三代对比（1024 振荡率 = 协议质量温度计）

| 协议 | jev 振荡率 | jev 分数 | 根因 |
|---|---|---|---|
| v1（中性描述） | ~98%* | 4 | 无信息描述 |
| v2（后果数据） | ~98%* | 4 | **游戏 bug：合并才生成新块 → 棋盘退化** |
| v3（修复游戏 + 层级 + fanout） | **7.5%** | **2444** | 全部修复 |

*v1/v2 的 1024 数据已被规则 bug 污染（棋盘退化为 2 块永冻），**v2 报告中"4 方向小动作空间是
公共盲区"的结论撤回**——真凶是环境 bug，不是模型。此教训单列。

## 四、过程发现：1024 规则 bug（本轮最重要产出）

**现象**：v3 下 jev 在 1024 仍 98% 振荡，但概率分布真实摆动（left 0.79 ↔ right 0.66），
frame10 与 frame30 棋盘完全相同——模型在认真决策一个**不会变化的世界**。

**根因**：`step()` 原为 `if gained: spawn()`（只有合并才生成新块）。标准 2048 规则是
**每次有效移动都生成**。首次合并后棋盘只剩两个异值块 → 任何方向都无合并 → 永无新块 → 死循环。

**修复**：`if changed: spawn()`（任何棋盘变化后生成）。随机策略即从"600 步 horizon 4 分"
变为"109-179 步自然终局 784-1624 分"，与真实 2048 动态一致。

**教训**：评测环境 bug 会伪装成模型缺陷。诊断顺序应是：先复现轨迹对齐（棋盘为何不变），
再怀疑 prompt。"所有模型都表现差"恰恰更可能是环境问题而非模型问题（幸存者偏差反向）。

## 五、验收

- 游戏单测全绿（含修复后 1024 动态）；Tetris v2 单测 6 项绿
- v3 12 局全录（tetris 沿用 + 1024 重录）；0 契约违约
- Playwright 8 项断言 PASS、零 console 错误（schema v1|v2|v3 兼容）
- 文本验证终局棋盘：jev 呈降幂蛇形结构（真策略形态），nanojev/laya 满盘碎块
- aux vision 通道 400 报错（provider 问题），改用棋盘 JSON 文本校验，结论等效

## 六、结论

1. **最佳实践把 1024 从"测不出差距"变成"清晰梯队"**：jev 2004 / laya 1334 / nanojev 622
2. **策略层级指令是第一杠杆**（laya 都因此开张消行），fan-out 与组合评分是增量收益
3. v1→v3 的 jev 分数轨迹：4 → 4 → 2444，其中游戏修复 > 指令改造 > fan-out（按贡献排序）
4. 方法论沉淀：**"三家都烂"先查环境，"一家独好"再归因模型**
