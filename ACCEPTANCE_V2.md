# Jev Arena V2 验收报告（Round 2 复测）

日期：2026-09-20 · v1 遗留边界项修复 + 全量复测 · schema `jev-arena-v2`

## 一、Round 2 改动（针对 v1 验收报告"已知边界"）

| v1 边界 | v2 修复 |
|---|---|
| Tetris 只有列选择（固定朝向） | **TetrisV2**：`(朝向×列)` 组合动作空间（I 17 选项、T 34 截断到 18），朝向集合去重（O=1、I/S/Z=2、T/J/L=4），硬掉落+洞数/消行后果进描述 |
| 1024 区分度不足 | 棋盘加 row 坐标标注；选项描述升级：合并数 + **最大合并产物** + 得分 + 空格变化 + **新最大块** |
| 无长程行为指标 | 新增 `oscillation_rate`（来回平移率）、`repeat_rate`、`direction_usage`、`orientation_usage`、`orientations_used` |
| 校验器锁 schema | 放宽 `jev-arena-v1|v2` 兼容 |

v1 录像已归档：`web/arena_results_v1.json` / `arena_slim_v1.json`（数据不覆盖，两版可对比）。

## 二、V2 对局结果（2 游戏 × 2 种子 × 3 引擎 = 12 局）

### Tetris（(朝向,列) 组合动作，80 块预算）

| 引擎 | seed 2026 | seed 901 | 平均分 | 消行 | 平均洞 | 朝向多样性 |
|---|---|---|---|---|---|---|
| **Jev** | **340 分 / 8 消行 / 49 块** | 80 分 / 2 消行 / 34 块 | **210** | **5.0** | **19.5** | **4/4 全用** |
| NanoJev | 0 分 / 17 块 | 0 分 / 20 块 | 0 | 0 | 57.5 | 3.5/4 |
| Laya | 0 分 / 23 块 | 0 分 / 17 块 | 0 | 0 | 56 | 3/4 |

**v1→v2 跃迁**：v1 里 jev 最好成绩 40 分 1 消行；v2 同 seed 下 340 分 8 消行——
旋转自由度放开后，强模型的领先被**放大 8.5 倍**。jev 的朝向使用 `{0°:11, 90°:30, 180°:3, 270°:5}`
显示它主动偏好竖放（插缝消行），这不是随机可达的行为特征。

### 1024（v2 描述 + 坐标标注，240 步预算）

| 引擎 | 均分 | 振荡率 | 方向分布（seed2026） |
|---|---|---|---|
| Jev | 4 | 98% | up 119 / down 119 / right 2 |
| NanoJev | 0 | 100% | left 120 / right 120 |
| Laya | 4 | 76% | 四方向相对均衡 (47/74/46/73) |

**关键发现（v1 测不出的新事实）**：加了"最大合并产物/新最大块"后，三家 1024 仍无实质提升——
但**振荡率指标揭示 jev 在 1024 上也近乎纯振荡（98%）**，说明 4 方向超小动作空间本身就是这类模型
的盲区（上下对称博弈无法从文本棋盘推断长期价值），与其在 Tetris 17-34 选项空间里的精准表现形成
鲜明对比。**动作空间大小与文本可推断性是 System-One 模型游戏评测的第一设计变量**——这是本项目
最重要的方法论结论。

### 契约质量

- 12 局 × 全部步：**0 次非法动作**（violation=0），jve/nanojev/laya 全程合法
- laya 在 17 选项下退化为 one-hot（100%/0%…），置信度虚高（1.0）——它的 softmax 温度问题，与 v1 冒烟一致
- nanojev 延迟上升（1.9s vs v1 0.24s）：本次 1024 局与 Bonsai2 服务在 GPU1 上争带宽，属环境噪声

## 三、复测验收（Playwright + vision 双通道）

- `verify_site.py` 8 项断言全过、零 console 错误（schema v2 兼容后）
- vision 实拍确认：朝向标签（"90° @ col 5"）正确渲染、jev 终局 8 消行棋盘与 nanojev/laya 的
  死局形态肉眼可辨、概率条多选项分布正常、Orients used 列（4/≥2 vs 3/≥2）落表
- 截图：`shots/04~06_tetris_*.png`（v2 Tetris 初始/中局/终局）

## 四、Round 2 弯路（4 项）

1. **旋转矩阵写错索引**（`_rot_cw` 用错维度）→ 单测 IndexError 即抓；修正为 `new[r][c]=old[R-1-c][r]`
2. **landing_top 起始高度不够**（-1 对多行 mask 不够深）→ 顶部虚格语义（y<0 视为空）修正
3. **测试假设"平 I 不能跨塔"想当然**——实际硬掉落下平 I 可以落在塔顶（row 5），描述如实暴露 16 洞代价。
   教训：物理规则类断言必须先跑一遍真实输出再写期望值
4. **seed 3/5/7/9 首块不是 I** → 扫描 seed 空间找 I 开局的种子（14/28/31/32），测试要服从实现细节时先探测

## 五、结论与后续

1. **Jev 在组合动作空间下优势扩大**（Tetris 210 vs 0 均分），旋转决策是真正的分水岭
2. **超小动作空间（4 方向）是这类模型的公共盲区**，1024 无法区分决策质量，只暴露行为癖好——
   后续如需 1024 区分度，应改为"列选择式"动作（选哪一列作为合并锚点）或提供价值评分问题
3. laya 的 one-hot 分布在两个游戏中一致出现，可作为其"无校准置信度"的定性证据
4. 建议下一轮：接入 openjev（NLI 范式）补齐四引擎对比；或用 NanoJev 的 games_gold variant 权重复测
