# Jev Arena V4 验收报告（Round 4：Tetris 策略与几何修复）

日期：2026-09-21 · schema `jev-arena-v3`（数据 v4：20 行棋盘 + 平坦度特征）

## 一、用户反馈的两个问题与诊断

**Q1：俄罗斯方块策略优化了吗？三家分数都不高。**

**Q2：web 里看到堆到顶了，游戏还继续好一会儿才结束。**

诊断（用 v3 录像逐帧分析，非猜测）：

1. **Q2 根因确认**：jev v3 局在 frame 21 就有方块接触 row 0，但又合法地玩了 **25 块**才死。
   原因不是死亡规则错（规则本身是标准的"新块放不下即死"），而是：
   - 棋盘只有 **14 行**（标准 20 行），容错空间极小；
   - jev 把 col9 压到高度 2、其余列堆到 14（frame 40 列高 `13,12,14,12,12,12,14,14,12,2`）——
     一个深坑让棋盘"看起来满了"但物理上还能塞。**这是策略不知道保持列高均衡的直接证据。**

2. **Q1 根因**：v3 的指令层级只有"消行>防洞>低堆"，没有任何**列间平坦度**信号；
   选项描述也没有聚合高度/起伏度数据；没有下一块预览。

## 二、V4 修复（全部围绕让模型能感知"平整"）

| 改动 | 内容 |
|---|---|
| 棋盘几何 | **14 → 20 行**（标准高度，容错空间恢复） |
| 指令层级 | 第 3 优先级扩为"每列低且均匀：stack-height sum 低、max column 低、bumpiness 低；**平整低面优于一柱冲天带深坑**" |
| 选项描述 | 每个落点追加 `stack height sum / max column / bumpiness`（Dellacherie 特征） |
| 状态文本 | 追加当前全局高度汇总 + **下一块预览**（含全部朝向掩码，"plan one move ahead"） |
| 组合重排 | 兜底重排键升级 `(clears↓, holes↑, sum↑, bump↑)` |
| 测试 | 全部单测适配 20 行；贪心基线从 2 消行升至 **15 消行 600 分**（游戏空间合理性锚点） |

## 三、V4 结果

### Tetris（20 行 + 平坦度信号）

| 引擎 | seed2026 | seed901 | 均分 | 消行 | 终局形态 |
|---|---|---|---|---|---|
| **Jev** | **1900 / 17 消行 / 80 块打满** | **1280 / 26 消行 / 80 块打满** | **1590** | 21.5 | seed901 终局仅 60 格填充、列高 4-9、顶行 11 ——**真正的低矮平整形态** |
| NanoJev* | 40 / 1 消行 | 560 / 13 消行 | 300 | 7 | 15+49 次越界选择→rerank 兜底 |
| Laya | 0 | 0 | 0 | 0 | 仍然速死（one-hot 分布不适合 17-34 选项） |

*nanojev 本轮踩中新坑（见下），分数是"模型+兜底"混合成绩，已如实标注。

**对比 v3（14 行）**：jev 340→1900（5.6 倍）。**两局都打满 80 块预算**，seed901 全程没碰到 row 0
（顶行 11）。"到顶还继续"现象消失（v3: frame21 触顶后再玩 25 块；v4 seed2026 触顶即接近终局）。

### 1024（不变，沿用 v3 修复后规则）

jev 1788/1564、laya 1072/1596、nanojev 644/600 —— 与 v3 一致（本轮未动 1024）。

## 四、本轮新坑：nanojev 512-token 硬上限

- 现象：nanojev tetris 局 violations=80、latency=0——根本没问模型，全是代码端 rerank 在玩。
- 根因：v4 状态加预览+特征后，选项路径文本 **679 token 超过 nanojev 的 max_length=512**，
  worker 明确报错"候选路径为 679 token，超过 max_length=512；未截断输入"（拒绝而非截断，好设计），
  但 runner 把空答案静默吞了。
- 修复：`render_state_for(engine,...)` 按引擎分派——nanojev 用 compact 状态（裁空行、去预览，
  212 字节 vs 1128）；**并且给 runner 加了"空答案=violation 计数"语义**（本次 violations=15/49
  是真实的 nanojev 越界选择，不是静默失败）。
- 教训：**多引擎评测里"某家分数异常"先查它的输入是否被静默降级**。1.6B 以下模型 512 上限
  是常态，状态渲染必须按引擎分级。

## 五、验收

- 单测全绿（tetris_v2 6 项 + games 7 项，全部适配 20 行）
- Playwright 8 项 PASS、零 console 错误
- 终局棋盘数值校验：jev seed901 filled=60/bump=12 vs laya filled=88/bump=25 vs v3 顶天形态
  ——策略形态差异肉眼可辨
- vision 辅助通道仍 400（provider 侧问题），以棋盘 JSON 校验等效替代

## 六、结论

1. 用户两个观察全部命中：14 行高度+无平坦度信号 = 低分+假性"顶而不死"。修复后 jev Tetris
   1590 均分（26 消行最佳局），**三家差距与真实能力排序一致**（jev >> nanojev > laya）
2. Tetris 策略的本质是**全局几何**（列高均衡/坑深），不是局部消行——System-One 模型需要
   在指令+数据里显式拿到这些特征才能表现
3. nanojev 的 512 上限是它在富信息评测里的硬天花板：信息量与模型容量的权衡是本地小模型的
   根本约束
